{
  "require": [
    "test/init.js",
    // Allow resolving imports based on tsconfig `baseUrl` and `paths`.
    "ts-node/register",
    "tsconfig-paths/register"
  ],
  "recursive": true,
  "reporter": "spec",
  "timeout": 60000
}
